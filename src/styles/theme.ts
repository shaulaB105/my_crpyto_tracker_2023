import {DefalutTheme} from "styled-components";
import {pallets} from "./pallets";

export const theme:DefalutTheme = {
    font : pallets.black.siam,
    bg : pallets.white.base,
    accent : pallets.blue.base,
    assist : pallets.black.squid,
};
