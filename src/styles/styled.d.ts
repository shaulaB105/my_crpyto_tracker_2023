import 'styled-components';

declare module 'styled-components'{
    export interface DefalutTheme{
        bg : string;
        font : string;
        accent : string;
        assist : string;
    }
}